# 项目开发、部署与同步指南

本文档包含三个核心部分：
1.  **如何在本地运行和构建项目**
2.  **如何将项目部署到 Cloudflare Pages**
3.  **如何与上游 GitHub 仓库保持同步**

> 本项目修改自：https://github.com/sub-store-org/Sub-Store-Front-End

---

## 🚀 Part 1: 本地开发与构建

在部署之前，你需要在本地安装依赖并构建出用于部署的静态文件。

```bash
# install dependencies
pnpm i

# run the server
pnpm dev

# build the app
pnpm build

# preview the built app
pnpm preview
```

---

## ☁️ Part 2: 部署到 Cloudflare Pages

本指南将引导你使用 Wrangler CLI 将构建好的 `dist` 文件夹一键部署到 Cloudflare Pages。

### 1. 准备工作

*   ✅ **安装 Node.js**: 建议使用 LTS 版本。
*   ✅ **安装 Wrangler**: Cloudflare 的命令行工具 (`npm install -g wrangler`)。
*   ✅ **拥有 Cloudflare 账号**。
*   ✅ **项目代码在本地**。

### 2. 部署步骤

#### 第一步：登录 Cloudflare

在终端中运行以下命令，它会打开浏览器引导你完成授权。

```powershell
wrangler login
```

#### 第二步：执行部署命令

进入项目根目录，运行 `deploy` 命令指向 `dist` 文件夹。

```powershell
wrangler pages deploy ./dist
```
*   **首次部署**: Wrangler 会引导你创建或关联一个 Pages 项目，按照提示操作即可。

### 3. (可选) 在 CI/CD 中自动化部署

你可以使用 GitHub Actions 在每次推送到 `main` 分支时自动构建和部署。

1.  在 GitHub 仓库的 `Settings` -> `Secrets and variables` -> `Actions` 中，添加一个名为 `CF_API_TOKEN` 的 Secret。Token 值可以从 Cloudflare 仪表盘创建。
2.  在你的项目中创建 `.github/workflows/deploy.yml` 文件：

```yaml
# .github/workflows/deploy.yml
name: Deploy to Cloudflare Pages
on:
  push:
    branches:
      - main # 仅在 main 分支更新时触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 安装 PNPM 用于依赖管理
      - name: Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: 8 # 指定 pnpm 版本

      # 安装 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18' # 指定 Node.js 版本
          cache: 'pnpm'

      # 安装依赖
      - name: Install Dependencies
        run: pnpm i

      # 构建项目
      - name: Build Project
        run: pnpm build

      # 部署到 Cloudflare Pages
      - name: Deploy
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: wrangler pages deploy ./dist
```

---

## 🔄 Part 3: 与上游 GitHub 仓库保持同步

当你将别人的公开项目上传到自己的仓库后，可以通过以下步骤轻松地从原作者（上游）那里获取更新。

### 1. 初始化设置 (仅需一次)

进入你的本地项目目录，我们将把**原始仓库**设置为 `upstream`，把你**自己的仓库**设置为 `origin`。

```powershell
# 1. (如果已存在) 重命名原始的 origin 为 upstream
git remote rename origin upstream

# 2. 添加你自己的 GitHub 仓库为新的 origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 3. 验证设置
git remote -v
```

### 2. 推送代码到你的仓库

将本地的分支推送到你的 GitHub 仓库。

```powershell
# 推荐保持本地和远程分支名一致 (例如都为 main)
# git push -u origin main

# 如果你本地是 master，想推送到远程的 main 分支
git push -u origin master:main
```

### 3. 日常同步流程

当你需要获取原始仓库的最新更新时，执行以下操作。

#### 第一步：从上游拉取最新代码

```powershell
git fetch upstream
```

#### 第二步：合并更新到你的分支

```powershell
git checkout main
git merge upstream/main
```
> **注意**: 如果你本地有修改，合并时可能会产生**冲突 (Conflict)**。你需要根据提示手动解决这些冲突，然后提交更改。

#### 第三步：将更新推送到你的仓库

```powershell
git push origin main
```